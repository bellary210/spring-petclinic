pipeline{
   agent any
     stages {
     stage('clone repo and clean it') {
        steps {
	       sh "git clone https://github.com/bellary210/spring-petclinic.git"
            }
         }
     stage('install') {
        steps {
          sh "mvn package"
          sh "java -jar target/*.jar"
            }
         }        
      }
}
