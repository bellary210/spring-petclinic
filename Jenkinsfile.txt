pipeline{
   agent any
     stages {
     stage('clone repo and clean it') {
        steps {
	       sh "git clone https://github.com/srinu541/spring-petclinic.git"
            }
         }
     stage('install') {
        steps {
          sh "mvn clean install -U"
            }
         }        
      }
}
